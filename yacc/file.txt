create database test;
use database test;

CREATE TABLE customer (
id int(10) NOT NULL,
name varchar(25),
gender varchar(1) NOT NULL,
check (gender in ('F','M')),
PRIMARY KEY (id)
);

insert into customer values
(1, 'name1', 'F'),
(2, 'name2', 'M'),
(3, '1name3', 'M'),
(4, '1name', 'M');

SELECT MIN(id), max(id) FROM customer where id = 1;
SELECT max(id) FROM customer;
SELECT sum(id) FROM customer;
SELECT avg(id) FROM customer group by id;

select * from customer where name = '%name%';
select * from customer where name = 'name%';
select * from customer where name = '%name';

desc table customer;